import React from 'react';

// import { Container } from './styles';

const home: React.FC = ({ children }) => {
  return (
    <>
      <h1>AUTH</h1>
      {children}
    </>
  );
};

export default home;
